#ifndef MINIRDBMS_PARSER_H
#define MINIRDBMS_PARSER_H

#include <bits/stdc++.h>
#include "Token.h"

class Parser {
 vector<Token> tokens;
public:
 Parser(vector<int> tokens);
};


#endif //MINIRDBMS_PARSER_H
