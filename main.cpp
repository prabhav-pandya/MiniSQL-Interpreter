#include <bits/stdc++.h>
#include "SQLInterpreter.h"

using namespace std;

int main() {
    string query;
    SQLInterpreter interpreter = SQLInterpreter();
    for(;;){
        cout<<"> ";
        cin>>query;
    }
    return 0;
}
